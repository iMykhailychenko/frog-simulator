/**
 * @desc DOM references
 */

const refs = {
    inputs: document.querySelectorAll('label input'),
    jumpBtn: document.getElementById('jump'),
    reproduceBtn: document.getElementById('reproduce'),
};

export default refs;
